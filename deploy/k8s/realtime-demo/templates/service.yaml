apiVersion: v1
kind: Service
metadata:
  name: {{ include "realtime-demo.fullname" . }}
  labels:
    {{- include "realtime-demo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "realtime-demo.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
